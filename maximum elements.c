#include <stdio.h>
int main() {
    int  max,i,a[i];
    printf("enter a size of array");
    scanf("%d",&a[i]);
    
    for(i=0;i<max;i++){
    
        scanf("%d",a[i]);
    }
    
   { max=a[i];
    printf("maximum element=%d",max);
}
    return 0;
}