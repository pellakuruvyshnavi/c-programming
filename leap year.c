#include <stdio.h>
int main() {
    int a;
    int b;
    scanf("%d,%d",&a,&b);
    if (a>b) {

    
    printf("give  a year");
    }
    else if {
    printf("enter a leap year");
    }
    else {
    printf("enter a not leap year");
    }
    return 0;
}