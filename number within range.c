#include <stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    if(n>=10 && n<=20){

    printf("number within the range");
    }
    else {
    printf("number not within the range");
    }
    
    return 0;
}          