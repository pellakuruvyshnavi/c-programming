#include <stdio.h>
int main() {
     int age;
     scanf("%d",&age);
     if (age>=18){
        printf("eligible vote ");
     }
     else {     
        printf("no eligible vote");
}
     
    
    return 0;
}