#include <stdio.h>


int main() {
     
     int a,b,c;
      a=10;

      printf ("the value of a BEFORE INCREMENT IS %d",a);
     /*increment A*/
     a=a+1;
     printf ("the value of BEFORE increment is %d",a);
    a=a-1;
    printf ("the value of BEFORE Decrement is %d",a);


     
    return 0;
}